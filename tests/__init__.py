# Package marker: guarantees the repository's own ``tests`` package wins
# over any stray site-packages ``tests`` directory (regular packages take
# precedence over namespace-package portions found earlier on sys.path).
