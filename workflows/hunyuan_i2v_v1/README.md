# hunyuan_i2v_v1 (stub)

Placeholder workflow directory (plan §4). The real `manifest.json` schema,
hashing, validation, and the ComfyUI graph template (`workflow.json`) are
delivered in M4 (Workflow Contract) and M5 (ComfyExecutor). v0.1 executes only
against `FakeExecutor` until the M3 hard gate passes (plan §2).
